;; The code was moved into lsp-mode.el. This file is kept only for backward compatibility.
(require 'lsp-mode)

;; (warn "Replace (require 'lsp) with (require 'lsp-mode)")

;; TODO: Remove this, this is test if we install it from source.
(defun lsp-test-packages () (message "[INFO ]Remove this after test."))

(provide 'lsp)

;;; lsp.el ends here
